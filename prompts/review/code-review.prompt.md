Use `$api-review`.

Review scope:

```text
<working tree, files, commit, or feature scope>
```

Compare specification, approved plan, production code, tests, configuration,
and actual command evidence. Delegate to `api_reviewer` in read-only mode.
Lead with concrete correctness/regression/security/test findings. Exclude
style-only, speculative, duplicate, and out-of-scope findings.
