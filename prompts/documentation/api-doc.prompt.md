Use `$api-document`.

Specification:

```text
spec/spec-api.md
```

Approved plan:

```text
docs/plans/<approved-plan>.md
```

Accepted review:

```text
docs/reviews/latest-review.md
```

Output:

```text
docs/api/<api-name>.md
```

Document only verified client-visible behavior. Do not modify source, tests,
specification, plan, or review report.
