Use `$api-document`.

Specification:

```text
spec/spec-api.md
```

Approved plan:

```text
docs/plans/2026-07-19-task-api-plan.md
```

Accepted review:

```text
docs/reviews/latest-review.md
```

Output:

```text
docs/api/task-api.md
```

Delegate to `api_doc_writer`.

Allowed ownership: `docs/api/` only.

Document all six endpoints, models, exact status values, error catalog, validation, examples, integration checklist, and verified limitations.

Do not invent authentication, pagination, sorting, database durability, retries, transitions, fields, statuses, or errors.

Expected handoff:

```text
READY_FOR_FINAL_VERIFICATION
```
