Use `$api-analyze`.

Investigate only. Remain read-only.

Observed behavior:

```text
<actual behavior>
```

Expected behavior and source:

```text
<specification/accepted requirement>
```

Evidence:

```text
<logs, stack trace, request/response, failing test>
```

Trace the real execution path, identify the root cause and regression surface,
separate code/test/config/environment causes, and return a focused fix plan.
Do not implement in this step.
