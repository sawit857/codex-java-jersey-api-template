Use `$api-workflow`.

Add endpoint:

```text
<METHOD> <PATH>
```

Purpose:

```text
<client-visible behavior>
```

Specification:

```text
spec/spec-api.md
```

First verify that the specification defines method, path, request/response models,
success status, errors, validation, and out-of-scope behavior.
Stop for contract ambiguity. Complete analysis, implementation, tests, review,
documentation, and final verification without changing unrelated endpoints.
